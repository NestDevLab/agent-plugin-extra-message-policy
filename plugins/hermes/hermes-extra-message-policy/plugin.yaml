name: hermes-extra-message-policy
version: 0.1.0
description: Hermes gateway message policy, silent ingest, and bounded raw recall.
provides_hooks:
  - pre_gateway_dispatch
  - pre_llm_call
provides_commands:
  - policy
